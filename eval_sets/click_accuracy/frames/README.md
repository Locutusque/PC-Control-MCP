Screenshots referenced by `../eval.jsonl` go here.

They are not committed: screenshots of real applications are captured data and
fall under the same handling rules as the training pool (see
`docs/privacy.md`). Keep them local, or use a synthetic UI.
